<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog/1.9"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="
                       http://www.liquibase.org/xml/ns/dbchangelog/1.9
                       http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-1.9.xsd">

     <changeSet author="lgayard" id="2012-07-01-14506">
        <comment>Configuration options to control user passwords</comment>
        <sql splitStatements="false">

insert into configuration (key, value,description, version) values ('pwd.chars.min',       '8',    'Password minimum length', 0);
insert into configuration (key, value,description, version) values ('pwd.chars.max',       '-1',   'Password maximum length', 0);
insert into configuration (key, value,description, version) values ('pwd.chars.case.lower', 'false', 'Whether the password must contain lowercase letters', 0);
insert into configuration (key, value,description, version) values ('pwd.chars.case.upper', 'false', 'Whether the password must contain upper case letters', 0);
insert into configuration (key, value,description, version) values ('pwd.chars.digits',      'false', 'Whether the password must contain digits', 0);
insert into configuration (key, value,description, version) values ('pwd.chars.specials',    'false', 'Whether the password must contain digits', 0);
insert into configuration (key, value,description, version) values ('pwd.allow.reuse',      'false', 'Whether the password can be equal to a previous password', 0);
insert into configuration (key, value,description, version) values ('pwd.history.size',     '1',    'How many old passwords to remember and to forbid repeating', 0);
        </sql>
    </changeSet>

     <changeSet author="lgayard" id="2012-07-01-14506-2">
        <comment>New table to store user's old passwords, so they cannot repeat passwords.</comment>
		<createTable tableName="user_old_password_hashes">
			<column autoIncrement="true" name="id" type="serial">
				<constraints nullable="false" primaryKey="true"/>
			</column>
			<column name="user_id"       type="int" />
			<column name="password_hash" type="varchar(255)"/>
		</createTable>
        <addForeignKeyConstraint constraintName="userid"
                                 referencedTableName="user_account"
                                 referencedColumnNames="user_id"
                                 baseColumnNames="user_id"
                                 baseTableName="user_old_password_hashes" />
    </changeSet>
</databaseChangeLog>
