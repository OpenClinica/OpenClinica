<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog/1.9"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog/1.9 http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-1.9.xsd">

     <changeSet author="drodrigues" id="2012-02-29-10549-1" dbms="postgresql">
        <comment></comment>
        <sql splitStatements="false">
<![CDATA[
create or replace view view_dn_dates as
select parent_dn_id, count(discrepancy_note_id) as "total_notes", min(date_created) as "date_created", max(date_created) as "date_updated" 
from discrepancy_note 
group by parent_dn_id 
having (parent_dn_id is not null and parent_dn_id <> 0);
]]>
        </sql>
    </changeSet>

     <changeSet author="drodrigues" id="2012-02-29-10549-2" dbms="postgresql">
        <comment></comment>
        <sql splitStatements="false">
<![CDATA[
create or replace view view_hidden_event_definition_crf as
SELECT 'study' as type, EDC.event_definition_crf_id, edc.hide_crf, edc.study_id, se.study_event_id, cv.crf_version_id
FROM EVENT_DEFINITION_CRF EDC, STUDY_EVENT SE, CRF_VERSION CV
WHERE EDC.study_event_definition_id=SE.study_event_definition_id
AND EDC.crf_id = CV.crf_id
AND EDC.parent_id is null
UNION
SELECT 'site' as type, EDC.event_definition_crf_id, edc.hide_crf, edc.study_id, se.study_event_id, cv.crf_version_id
FROM EVENT_DEFINITION_CRF EDC, STUDY_EVENT SE, CRF_VERSION CV
WHERE EDC.study_event_definition_id=SE.study_event_definition_id
AND EDC.crf_id = CV.crf_id
AND EDC.event_definition_crf_id not in
(select parent_id from event_definition_crf where parent_id is not null or parent_id <> 0);
]]>
        </sql>
    </changeSet>

     <changeSet author="drodrigues" id="2012-02-29-10549-3" dbms="postgresql">
        <comment></comment>
        <sql splitStatements="false">
<![CDATA[
create or replace view view_dn_item_data as select 
s.study_id, 
s.parent_study_id, 
vhstudy.hide_crf as "study_hide_crf",
vhsite.hide_crf as "site_hide_crf",
dn.discrepancy_note_id, 
ss.label, 
ss.status_id as "ss_status_id",
dn.discrepancy_note_type_id,
dn.resolution_status_id, 
s.unique_identifier as "site_id", 
dd.date_created,
dd.date_updated,
da.days, 
da.age, 
sed.name as "event_name", 
se.date_start, 
c.name as "crf_name", 
ec.status_id, 
i.name as "entity_name", 
id.value, 
dn.entity_type, 
dn.description, 
dn.detailed_notes, 
dd.total_notes,
ua.first_name, 
ua.last_name, 
ua.user_name,
ua2.first_name as "owner_first_name", 
ua2.last_name as "owner_last_name", 
ua2.user_name as "owner_user_name"
from discrepancy_note dn left outer join user_account ua on (
    dn.assigned_user_id = ua.user_id
), user_account ua2, dn_item_data_map map, dn_age_days da, study_subject ss, study s, item_data id, item i, event_crf ec, study_event se, study_event_definition sed, crf_version cv, crf c, view_hidden_event_definition_crf vhstudy, view_hidden_event_definition_crf vhsite, view_dn_dates dd
where
dn.owner_id = ua2.user_id
and dn.discrepancy_note_id = map.discrepancy_note_id
and map.study_subject_id = ss.study_subject_id
and map.item_data_id = id.item_data_id
and dn.discrepancy_note_id = da.discrepancy_note_id
and id.item_id = i.item_id
and id.event_crf_id = ec.event_crf_id
and ec.study_event_id = se.study_event_id
and se.study_event_definition_id = sed.study_event_definition_id
and ec.crf_version_id = cv.crf_version_id
and cv.crf_id = c.crf_id
and ss.study_id = s.study_id
and ((dn.parent_dn_id is null) or (dn.parent_dn_id = 0))
and vhstudy.type = 'study'
and vhstudy.study_event_id = se.study_event_id
and vhstudy.crf_version_id = cv.crf_version_id 
and vhsite.type = 'site'
and vhsite.study_event_id = se.study_event_id
and vhsite.crf_version_id = cv.crf_version_id
and dn.discrepancy_note_id = dd.parent_dn_id;
]]>
        </sql>
    </changeSet>

     <changeSet author="drodrigues" id="2012-02-29-10549-4" dbms="postgresql">
        <comment></comment>
        <sql splitStatements="false">
<![CDATA[
create or replace view view_dn_event_crf as 
select 
s.study_id, 
s.parent_study_id, 
vhstudy.hide_crf as "study_hide_crf", 
vhsite.hide_crf as "site_hide_crf",
dn.discrepancy_note_id, 
ss.label, 
ss.status_id as "ss_status_id",
dn.discrepancy_note_type_id,
dn.resolution_status_id, 
s.unique_identifier as "site_id", 
dd.date_created,
dd.date_updated,
da.days, 
da.age, 
sed.name as "event_name", 
se.date_start, 
c.name as "crf_name", 
ec.status_id, 
TRIM('') as "entity_name", 
TRIM('') as "value", 
dn.entity_type, 
dn.description, 
dn.detailed_notes, 
dd.total_notes,
ua.first_name, 
ua.last_name, 
ua.user_name,
ua2.first_name as "owner_first_name", 
ua2.last_name as "owner_last_name", 
ua2.user_name as "owner_user_name"
from
discrepancy_note dn left outer join user_account ua on (
    dn.assigned_user_id = ua.user_id
), user_account ua2, dn_event_crf_map map, event_crf ec, study_event se, study_subject ss, study s, dn_age_days da, study_event_definition sed, crf_version cv, crf c, view_hidden_event_definition_crf vhstudy, view_hidden_event_definition_crf vhsite, view_dn_dates dd
where
dn.discrepancy_note_id = ua2.user_id
and map.discrepancy_note_id = dn.discrepancy_note_id
and map.event_crf_id = ec.event_crf_id
and ec.study_event_id = se.study_event_id
and ec.study_subject_id = ss.study_subject_id
and ss.study_id = s.study_id
and dn.discrepancy_note_id = da.discrepancy_note_id
and se.study_event_definition_id = sed.study_event_definition_id
and ec.crf_version_id = cv.crf_version_id
and cv.crf_id = c.crf_id
and ((dn.parent_dn_id is null) or (dn.parent_dn_id = 0))
and vhstudy.type = 'study'
and vhstudy.study_event_id = se.study_event_id
and vhstudy.crf_version_id = cv.crf_version_id 
and vhsite.type = 'site'
and vhsite.study_event_id = se.study_event_id
and vhsite.crf_version_id = cv.crf_version_id
and dn.discrepancy_note_id = dd.parent_dn_id;
]]>
        </sql>
    </changeSet>

     <changeSet author="drodrigues" id="2012-02-29-10549-5" dbms="postgresql">
        <comment></comment>
        <sql splitStatements="false">
<![CDATA[
create or replace view view_dn_study_event as 
select 
s.study_id, 
s.parent_study_id, 
false as "study_hide_crf", 
false as "site_hide_crf",
dn.discrepancy_note_id, 
ss.label, 
ss.status_id as "ss_status_id",
dn.discrepancy_note_type_id, 
dn.resolution_status_id, 
s.unique_identifier as "site_id",
dd.date_created,
dd.date_updated,
da.days, 
da.age,
sed.name as "event_name", 
se.date_start,
TRIM('') as "crf_name",
0 as "status_id",
TRIM('') as "entity_name", 
TRIM('') as "value", 
dn.entity_type,
dn.description,
dn.detailed_notes,
dd.total_notes,
ua.first_name, 
ua.last_name, 
ua.user_name,
ua2.first_name as "owner_first_name", 
ua2.last_name as "owner_last_name", 
ua2.user_name as "owner_user_name"
from
discrepancy_note dn left outer join user_account ua on (
    dn.assigned_user_id = ua.user_id
), user_account ua2, dn_study_event_map map, study_event se, study_subject ss, study s, dn_age_days da, study_event_definition sed, view_dn_dates dd
where
dn.owner_id = ua2.user_id
and map.discrepancy_note_id = dn.discrepancy_note_id
and map.study_event_id = se.study_event_id
and se.study_subject_id = ss.study_subject_id
and ss.study_id = s.study_id
and dn.discrepancy_note_id = da.discrepancy_note_id
and se.study_event_definition_id = sed.study_event_definition_id
and ((dn.parent_dn_id is null) or (dn.parent_dn_id = 0))
and dn.discrepancy_note_id = dd.parent_dn_id;
]]>
        </sql>
    </changeSet>

     <changeSet author="drodrigues" id="2012-02-29-10549-6" dbms="postgresql">
        <comment></comment>
        <sql splitStatements="false">
<![CDATA[
create or replace view view_dn_study_subject as 
select 
s.study_id, 
s.parent_study_id, 
false as "study_hide_crf", 
false as "site_hide_crf",
dn.discrepancy_note_id, 
ss.label, 
ss.status_id as "ss_status_id",
dn.discrepancy_note_type_id, 
dn.resolution_status_id, 
s.unique_identifier as "site_id",
dd.date_created,
dd.date_updated,
da.days, 
da.age, 
TRIM('') as "event_name", 
null::date as "date_start",
TRIM('') as "crf_name",
0 as "status_id", 
TRIM('') as "entity_name", 
TRIM('') as "value", 
dn.entity_type,
dn.description, 
dn.detailed_notes,
dd.total_notes,
ua.first_name, 
ua.last_name, 
ua.user_name,
ua2.first_name as "owner_first_name", 
ua2.last_name as "owner_last_name", 
ua2.user_name as "owner_user_name"
from
discrepancy_note dn left outer join user_account ua on (
    dn.assigned_user_id = ua.user_id
), user_account ua2, dn_study_subject_map map, study_subject ss, study s, dn_age_days da, view_dn_dates dd
where
dn.owner_id = ua2.user_id
and map.discrepancy_note_id = dn.discrepancy_note_id
and map.study_subject_id = ss.study_subject_id
and ss.study_id = s.study_id
and dn.discrepancy_note_id = da.discrepancy_note_id
and ((dn.parent_dn_id is null) or (dn.parent_dn_id = 0))
and dn.discrepancy_note_id = dd.parent_dn_id;
]]>
        </sql>
    </changeSet>

     <changeSet author="drodrigues" id="2012-02-29-10549-7" dbms="postgresql">
        <comment></comment>
        <sql splitStatements="false">
<![CDATA[
create or replace view view_dn_subject as 
select 
s.study_id, 
s.parent_study_id, 
false as "study_hide_crf", 
false as "site_hide_crf",
dn.discrepancy_note_id,
ss.label, 
ss.status_id as "ss_status_id",
dn.discrepancy_note_type_id, 
dn.resolution_status_id, 
s.unique_identifier as "site_id",
dd.date_created,
dd.date_updated,
da.days, 
da.age, 
TRIM('') as "event_name", 
null::date as "date_start",
TRIM('') as "crf_name", 
0 as "status_id", 
TRIM('') as "entity_name", 
TRIM('') as "value", 
dn.entity_type,
dn.description,
dn.detailed_notes,
dd.total_notes,
ua.first_name, 
ua.last_name, 
ua.user_name,
ua2.first_name as "owner_first_name", 
ua2.last_name as "owner_last_name", 
ua2.user_name as "owner_user_name"
from
discrepancy_note dn left outer join user_account ua on (
    dn.assigned_user_id = ua.user_id
), user_account ua2, dn_subject_map map, study_subject ss, study s, subject su, dn_age_days da, view_dn_dates dd
where
dn.owner_id = ua2.user_id
and map.discrepancy_note_id = dn.discrepancy_note_id
and map.subject_id = su.subject_id
and ss.subject_id = su.subject_id
and ss.study_id = s.study_id
and dn.discrepancy_note_id = da.discrepancy_note_id
and ((dn.parent_dn_id is null) or (dn.parent_dn_id = 0))
and dn.discrepancy_note_id = dd.parent_dn_id;
]]>
        </sql>
    </changeSet>

     <changeSet author="drodrigues" id="2012-02-29-10549-8" dbms="postgresql">
        <comment></comment>
        <sql splitStatements="false">
<![CDATA[
create or replace view view_discrepancy_note as 
select * from view_dn_subject
union
select * from view_dn_study_subject
union
select * from view_dn_study_event
union
select * from view_dn_event_crf
union
select * from view_dn_item_data;
]]>
        </sql>
    </changeSet>

</databaseChangeLog>     